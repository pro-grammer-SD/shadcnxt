"use strict";
/* eslint-disable no-redeclare, import/export */
exports.__esModule = true;
exports.pragma = void 0;
// Reference: https://developer.mozilla.org/en-US/docs/Glossary/Empty_element
var EMPTY_ELEMENT_LIST = [
    'area',
    'base',
    'br',
    'col',
    'embed',
    'hr',
    'img',
    'input',
    // 'keygen',
    'link',
    'meta',
    'param',
    'source',
    'track',
    'wbr'
];
function renderAttributes(attr) {
    return Object
        .entries(attr)
        .map(function (_a) {
        var _b, _c;
        var key = _a[0], value = _a[1];
        return (typeof value === 'boolean'
            ? key
            : key + "=\"" + ((_c = (_b = value) === null || _b === void 0 ? void 0 : _b.toString().replace(/&/g, '&amp;').replace(/"/g, '&quot;')) !== null && _c !== void 0 ? _c : '') + "\"");
    })
        .join(' ');
}
function pragma(tagName, properties) {
    var children = [];
    for (var _i = 2; _i < arguments.length; _i++) {
        children[_i - 2] = arguments[_i];
    }
    return (tagName in EMPTY_ELEMENT_LIST
        ? "<" + tagName + (properties ? " " + renderAttributes(properties) : '') + ">"
        : "<" + tagName + (properties ? " " + renderAttributes(properties) : '') + ">" + children.join('') + "</" + tagName + ">");
}
exports.pragma = pragma;
//# sourceMappingURL=index.js.map